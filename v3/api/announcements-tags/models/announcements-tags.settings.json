{
  "kind": "collectionType",
  "collectionName": "tags",
  "info": {
    "name": "Announcements_tags"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "text": {
      "type": "enumeration",
      "enum": [
        "Frontend",
        "Backend",
        "Doc",
        "Bug"
      ],
      "required": true,
      "unique": true
    },
    "value": {
      "type": "enumeration",
      "enum": [
        "frontend",
        "backend",
        "doc",
        "bug"
      ],
      "required": true
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "warning",
        "success",
        "danger"
      ],
      "required": true
    }
  }
}
